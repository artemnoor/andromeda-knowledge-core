# ADR 0002: Rules are data

Status: accepted.

Normative education policy changes more often than application code. Conditions,
effects, scopes, exceptions, provenance, tests and lifecycle are persisted rule
data so a threshold or a new DSL-composable policy can change without editing
Python business logic. Activation remains a controlled reviewer operation.

